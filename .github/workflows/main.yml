name: Lint, test, and build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: Borales/actions-yarn@master
    - name: Lint
      run: yarn lint
    - name: Test
      run: yarn test
    - name: Build
      run: yarn build
